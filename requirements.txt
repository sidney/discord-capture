# Python dependencies for discord-capture pipeline
# Install with: pip install -r requirements.txt --break-system-packages

numpy          # Fast cosine similarity in cluster_embed.py
oci            # OCI Vault secret resolution (instance principal auth)
